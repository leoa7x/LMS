{
  "name": "lms",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:api & npm run dev:web",
    "dev:web": "npm run dev -w @lms/web",
    "dev:api": "npm run start:dev -w @lms/api",
    "build:web": "npm run build -w @lms/web",
    "build:api": "npm run build -w @lms/api",
    "build": "npm run build:api && npm run build:web",
    "lint:web": "npm run lint -w @lms/web",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "compose:up": "docker compose up -d",
    "compose:down": "docker compose down",
    "start:web": "npm run start -w @lms/web",
    "start:api": "npm run start -w @lms/api",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:seed": "prisma db seed"
  },
  "devDependencies": {
    "@prisma/client": "^6.7.0",
    "bcryptjs": "^3.0.2",
    "prisma": "^6.7.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
