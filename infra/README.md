# Infra

Configuracion de entorno local y despliegue inicial del LMS.

## Estado

La infraestructura local ya soporta:

- `postgres` con Docker Compose
- `api` NestJS en contenedor
- `web` Next.js en contenedor

## Referencias

- compose principal: `docker-compose.yml`
- variables base: `.env.example`
- guia operativa: `docs/despliegue-local.md`
